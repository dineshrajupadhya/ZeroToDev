#include <stdio.h>
#include<stdlib.h>
#include<time.h>//used to define the time parameter in the code
int main() {
  int number,i,j;
  printf("Enter the number of rows:\n");
  scanf("%d",&number);
  for(i=1;i<=number;i++)/*this is used to print the number of rows it will have(based on the user provided)*/
{
      for(j=number;j>=i;j--)/*this is used to print the number of times the pattern should show in the line*/
      {
          printf("*");
      }
      printf("\n");
  }
    return 0;
}

Output:
Enter the number of rows:
7
*******
******
*****
****
***
**
*
